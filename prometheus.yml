global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'fleet-tracking-backend'
    metrics_path: '/api/actuator/prometheus'
    static_configs:
      - targets: ['backend:8080']  # ← DOCKER SERVICE NAME
        labels:
          service: 'fleet-tracking'
          environment: 'docker'